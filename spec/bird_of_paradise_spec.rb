require 'spec_helper'

describe BirdOfParadise do
  it 'has a version number' do
    expect(BirdOfParadise::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
